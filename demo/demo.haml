!!!
%body
	%head
		%title Restful Form
		%script{ :src => 'http://code.jquery.com/jquery-latest.js' }
		%script{ :src => '/jquery.restful-forms.js' }
		%link{ :href => '/style.css', :rel => 'stylesheet' }

	%body

		#flash= @flash

		#methods
			-%w{GET POST PUT DELETE}.each do |method|
				%fieldset
					%legend= "&lt;form method=\"#{method}\"&gt;"
					%form{ :action => '/path', :method => method }
						%button Submit
					%form.restful{ :action => '/resource', :method => method }
						%button RESTful Submit
					%form.restful{ :action => '/resource', :method => method, :target => 'my_frame' }
						%button RESTful Submit to Frame
					%form.restful{ :action => '/resource', :method => method, :target => '#flash < #flash' }
						%button RESTful Submit &amp; Load
			.clearfix

		%iframe{ :name => 'my_frame' }
		%fieldset
			%legend= "With fields"
			%form.restful{ :action => '/resource', :method => :put }
				%input{ :name => :test_field1, :type => :text, :value => 'test_value' }
				%button Submit

		#readme= @README.to_html
